networks:
  tutor-net:
    driver: bridge

volumes:
  pgdata:

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: nerdy_tutor
      POSTGRES_USER: ${DB_USER:-tutor}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-tutor_dev}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - tutor-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tutor"]
      interval: 5s
      timeout: 3s
      retries: 5

  agent:
    build:
      context: ./agent
      dockerfile: Dockerfile
    env_file:
      - path: .env
        required: true
    ports:
      - "8080:8080"
    restart: unless-stopped
    networks:
      - tutor-net
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request, sys; req = urllib.request.urlopen('http://localhost:8080/health', timeout=4); sys.exit(0 if req.status == 200 else 1)",
        ]
      interval: 30s
      timeout: 5s
      start_period: 15s
      retries: 3
    volumes:
      # Mount source for hot-reload in development
      - ./agent/src:/app/src:ro
      - ./agent/main.py:/app/main.py:ro

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    env_file:
      - path: .env
        required: true
    ports:
      - "3000:3000"
    restart: unless-stopped
    networks:
      - tutor-net
    depends_on:
      agent:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "const http = require('http'); const req = http.get('http://localhost:3000/api/health', (res) => process.exit(res.statusCode === 200 ? 0 : 1)); req.on('error', () => process.exit(1));",
        ]
      interval: 30s
      timeout: 5s
      start_period: 20s
      retries: 3
